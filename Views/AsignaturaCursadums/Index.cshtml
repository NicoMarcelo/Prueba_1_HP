@model IEnumerable<Prueba_1_HP.Models.AsignaturaCursadum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AsignaturasCodigoAsignaturaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstudiantesRutEstudianteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AsignaturasCodigoAsignaturaNavigation.CodigoAsignatura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstudiantesRutEstudianteNavigation.RutEstudiante)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAsignaturaCursada">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAsignaturaCursada">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAsignaturaCursada">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
